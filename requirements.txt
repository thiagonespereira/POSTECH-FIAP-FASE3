# Core
python-dotenv>=1.0.0

# Fine-tuning (LLM)
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
peft>=0.7.0
accelerate>=0.25.0
trl>=0.7.0
bitsandbytes>=0.41.0

# LangChain / LangGraph (usado no Step 5 – assistente; opcional para fine-tuning e avaliação)
langchain>=0.1.0
langchain-community>=0.0.20
# langgraph: versão no PyPI (0.0.8) está yanked. Instale depois quando for fazer o assistente:
#   pip install langgraph   ou  pip install git+https://github.com/langchain-ai/langgraph.git
langchain-huggingface>=0.0.1

# Evaluation / utils
scikit-learn>=1.3.0
tqdm>=4.65.0

# Optional (Colab / OpenAI fallback)
# langchain-openai>=0.0.5
